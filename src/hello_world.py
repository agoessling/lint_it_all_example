# Unused import
import os

# Lint violation, which is not auto-fixable.
# When running with `--fix` this one should be reported and lint should exit 1.
print("{".format("something"))

print("Hello World!")
